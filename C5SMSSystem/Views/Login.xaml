<UserControl x:Class="C5SMSSystem.Views.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:l="clr-namespace:C5SMSSystem"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Background>
        <RadialGradientBrush>
            <GradientStop Color="#FFE8E8E8" Offset="0" />
            <GradientStop Color="#FF909090" Offset="1" />
            
                <!--Satin Blue
            <GradientStop Color="#FF11B4ED" Offset="0" />
            <GradientStop Color="#FF0186BF" Offset="1" />-->
        </RadialGradientBrush>
    </UserControl.Background>
    <UserControl.Resources>
        <l:CountToBoolConverter x:Key="CountConverter"/>
        <LinearGradientBrush x:Key="TextBox_Border" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFABADB3" Offset="0.05" />
            <GradientStop Color="#FFE2E3EA" Offset="0.07" />
            <GradientStop Color="#FFE4E9EF" Offset="1" />
        </LinearGradientBrush>
    </UserControl.Resources>
    <Grid SnapsToDevicePixels="True" >
        <Border Visibility="{Binding Visibility}">
            <Border BorderBrush="Black" BorderThickness="1" Background="White" 
                CornerRadius="10,0,10,0" VerticalAlignment="Center"
                HorizontalAlignment="Center" Height="200" Width="250">
                <Border.BitmapEffect>
                    <DropShadowBitmapEffect Color="Black" 
                  Opacity="0.5" Direction="270" 
                  ShadowDepth="0.7" />
                </Border.BitmapEffect>
                    <Grid Margin="10" FocusManager.FocusedElement="{Binding ElementName=txtPassword}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                        </Grid.RowDefinitions>
                        <Grid Margin="20">
                            <Image x:Name="imgSMSLogo" Source="/C5SMSSystem;component/Resources/smsLogo_256.png" />
                        </Grid>
                        <Label Grid.Row="1" Content="Please enter password to unlock :" />
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <PasswordBox x:Name="txtPassword" l:PasswordBoxExtension.BindPassword="True" l:PasswordBoxExtension.BoundPassword="{Binding Path=Password,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" MaxLength="8" MinWidth="120"/>
                            <Button x:Name="btnOkay" Grid.Column="2" Content="OK" Width="60" Command="{Binding Path=CloseCommand}" />
                        </Grid>
                    </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
