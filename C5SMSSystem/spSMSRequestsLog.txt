IF NOT EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[spSMSRequestsLog]') AND type in (N'P', N'PC'))
exec(' CREATE Proc [dbo].[spSMSRequestsLog] 
	--@SMSRequestsLogID BIGINT,
	@MessageID BIGINT,
	@RequestNumber VARCHAR(20),
	@SentMessage VARCHAR(640),
	@SentStatus VARCHAR(20),
	@SentDateTime DATETIME

	WITH ENCRYPTION
AS
BEGIN
	-- SET NOCOUNT ON added to prevent extra result sets from
	-- interfering with SELECT statements.
	SET NOCOUNT ON;
			 
    -- Insert statements for procedure here
	INSERT INTO dbo.SMSRequestsLog
	        ( 
				MessageID ,RequestNumber ,SentMessage ,SentStatus, SentDateTime 
	        )
	VALUES  ( 
				@MessageID ,@RequestNumber ,@SentMessage ,@SentStatus, @SentDateTime
	        );
END')